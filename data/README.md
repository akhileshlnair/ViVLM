# Data

This directory contains mirrored medical image datasets and normalized SFT-style records.

- `raw/` holds the original images, tracked with Git LFS.
- `train/` holds JSONL records that point at the raw images and provide prompts/targets.
- `manifests/` tracks the source datasets and row counts.

